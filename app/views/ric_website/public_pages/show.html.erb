<h1><%= @page.title %></h1>
<% @page.page_blocks.order(position: :asc).each do |page_block| %>
	<div class="box">
		<% if !page_block.title.blank? %>
			<h2><%= page_block.title %></h2>
		<% end %>
		<%= page_block.content.html_safe if page_block.content %>
	</div>
<% end %>