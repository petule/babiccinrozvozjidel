<%= rug_menu_for(@delivery_map_zone, name: "actions", path_base: "main_app.admin_delivery_map_zone") do |m| %>
	<%= m.new_item nil, new_admin_delivery_map_zone_path(delivery_map_id: @delivery_map_zone.delivery_map_id) %>
	<%= m.show_item %>
	<%= m.edit_item %>
	<%= m.destroy_item %>
<% end %>