<%= rug_form_for(@tag, create_url: "main_app.admin_tags_path", update_url: "main_app.admin_tag_path") do |f| %>
	<%= f.text_input_row :name, :text_field %>
	<%= f.primary_button_row :submit %>
<% end %>