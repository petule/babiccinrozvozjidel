<%= rug_show_table_for(@page, { 
	parent: { type: :belongs_to, label: :title, path: :page_path }
}) %>
<h3><%= t("headers.ric_website.admin_pages.children") %></h3>
<%= rug_index_table_for(@page.children.order(lft: :asc), { 
	title: :string,
	url: :string,
}, paths: {
	show: :page_path,
	edit: :edit_page_path,
	destroy: :page_path
} ) %>